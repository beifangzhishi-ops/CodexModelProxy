# CodexModelProxy 项目规则

## 更新与推送

- 代码与文档更新默认提交并推送到 `origin/beta`，不直接推送到 `origin/master`。
- 仅当用户明确指示合并时，才将 `beta` 合并到 `origin/master` 并推送。
- 每次更新（包括拉取远程改动、修改代码或文档）完成后，都必须提交并推送；除非用户明确要求不推送，否则不得只提交不推送。
